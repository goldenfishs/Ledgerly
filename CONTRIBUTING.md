# 参与贡献

感谢帮助改进 Ledgerly。请先阅读 [README](README.md) 和[验证说明](docs/verification.md)，在独立数据目录中进行开发与测试。

## 开发与提交

1. 为问题或功能建立分支，说明具体触发条件与预期行为。
2. 保持修改范围清晰。账本和身份权限应在后端验证，不能只依靠隐藏前端按钮。
3. 对权限、金额、数据迁移、并发与文件处理修改增加有意义的回归测试。
4. 运行相关测试、前端格式检查与构建，记录实际结果及未覆盖部分。
5. 提交 Pull Request 时说明问题、最终行为、验证方式与可能的数据迁移影响。

常用检查：

```sh
python -m pytest -q
node --test tests/test_frontend.mjs
npm run format:check --prefix frontend
npm run build --prefix frontend
```

格式修正可使用 `npm run format --prefix frontend`，提交前检查是否产生无关文件改动。

## 数据与外部服务

- 使用虚构账号、邮箱、账单与票据。不要提交真实发票、数据备份、数据库、头像或登录会话。
- 不要提交 `.env`、SMTP 授权码、API Key 或 `system.key`。示例应使用占位值或保留测试域名。
- 测试 SMTP 和 AI 时使用模拟或受控服务，不让自动化测试依赖外部真实邮件或付费识别。
- 修改数据库结构时验证已有数据迁移、重复启动和失败回滚，不通过重新初始化规避问题。

## 报告问题

请提供使用版本、部署方式、浏览器、复现步骤、预期结果与实际结果。截图和日志应去除账号、邮箱、票据内容及授权信息。

发现可能泄露数据或绕过权限的问题时，请优先使用仓库提供的私密安全报告渠道；如果未启用该渠道，不要在公开 Issue 中附真实凭证或可利用的业务数据。
